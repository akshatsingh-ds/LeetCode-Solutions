class Solution(object):
    def findPairs(self, nums, k):
        if k < 0:
            return 0
        
        if k == 0:
            return sum(nums.count(x) > 1 for x in set(nums))
        
        seen = set(nums)
        return sum(1 for x in seen if x + k in seen)